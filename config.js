// config.js — 全局配置与状态常量
// 【V17 安全改动】数据库连接串不再硬编码，一律从环境变量读取（服务器后台或 .env 文件）。
//   必填：MONGO_URI   —— 数据库连接串
//   建议：JWT_SECRET  —— 登录令牌密钥（见 lib/core.js）
export const CONFIG = {
  appVersion: '20.1',
  mongoUri: process.env.MONGO_URI || '',
  dbName: process.env.MONGO_DB || 'invest-jiedanyuan',
  collection: 'orders',
  port: process.env.PORT || 3000,
};

// 启动自检：数据库连接串没配就直接停下，避免服务起来了却连不上库、报一堆看不懂的错
export function assertConfig() {
  if (!String(process.env.MONGO_URI || '').trim()) {
    console.error('============================================================');
    console.error('[配置] 没有检测到环境变量 MONGO_URI，无法连接数据库，服务已停止。');
    console.error('');
    console.error('  怎么配（任选一种）：');
    console.error('  1) 在服务器环境变量里加一条：');
    console.error('       MONGO_URI = mongodb+srv://用户名:密码@集群地址/数据库名?retryWrites=true&w=majority');
    console.error('     Render：控制台 → 选中你的服务 → Environment → Add Environment Variable');
    console.error('     宝塔：Node 项目 → 环境变量');
    console.error('  2) 在项目根目录新建 .env 文件，写一行：MONGO_URI=上面那串');
    console.error('');
    console.error('  提示：这串地址原来写在 config.js 里，需要你从旧文件或 MongoDB Atlas');
    console.error('       后台重新拿一次（建议顺便更换数据库密码）。');
    console.error('============================================================');
    process.exit(1);
  }
}

// 状态定义：「已交付」=「待结算」（旧数据自动归一化）
export const STATUSES = ['待开始', '进行中', '待结算', '已结算'];
export const DONE_STATUSES = ['待结算', '已结算'];   // 完单口径
export const CARD_STATUSES = ['待接单', '已接单', '待审核', '待打款', '已完成', '已拒绝', '已驳回'];
export const normalizeStatus = (s) => (s === '已交付' ? '待结算' : (STATUSES.includes(s) ? s : null));
export const normCard = (c) => ({ ...c, status: c.status === '已交付' ? '待打款' : c.status });
export const localToday = () => {
  const d = new Date();
  return d.getFullYear() + '-' + String(d.getMonth() + 1).padStart(2, '0') + '-' + String(d.getDate()).padStart(2, '0');
};

// 【2026-09-15】版本与更新日志（/api/version 供前端检查更新）
export const CHANGELOG = [
  { ver: '20.1', date: '2026-09-18', notes: ['管理后台新增「重置用户密码」：用户忘记密码时，管理员在「网站安全与用户管理 → 平台账号」一键生成临时密码（或指定新密码），转告用户即可登录', '说明：密码为加密存储，明文任何人都不可查看——找回密码统一走重置流程，每次重置自动写入审计日志', '用户列表新增密码状态列（原始密码 / 已重置时间）', '同步包含 V20 完整核验版全部内容：安全修复、性能优化、作品案例、文案馆、管理员后台整合'] },
{ ver: '1.5.5', date: '2026-09-17', notes: ['管理员后台整合：新增「管理工作台」门户（admin.html），五个板块方块式入口——台账 / 派单工作台 / 游戏控制器 / 用户端配置 / 网站安全与用户管理', '新增「网站安全与用户管理」页：平台账号列表（脱敏）、手机号/工号查人、站内信群发与定向、部署文件核验、危险操作（清对局/清钥匙/清聊天需二次确认）', '管理员登录后统一先进入管理工作台，各管理页顶部互相打通返回', '游戏管控台原来"返回工作台"错误指向写手页，已修正', '【终审】上线前最后一轮全量审查：修复 LV1 月度激励可能被误判停发、部署核验清单补齐 3 个后端文件、注册异常补偿（删残留账号再放邀请码）、用户列表补排序——共 42 个关键文件核验齐全'] },
{ ver: '1.5.3', date: '2026-09-17', notes: ['用户端登录后页面全面改版：从"工作台"变为「文案馆」，定位从办公转向消费购买', '新结构三段式：① AI 助手（默认首页，进来即见小沐，可查套餐/查优惠）② 商城（套餐购买+优惠提示条）③ 我的（资料+咨询进度+退出）', '导航从 4 项精简为 3 项，移动端底部 Tab 同步改版', '注册/登录完成后的欢迎语、顶部按钮全部改为"欢迎进入文案馆"，不再出现"工作台"字样', '小沐AI 升级 guide-v2：新增优惠查询——实时读取套餐划线价算出立减金额，会员券包提示为后续优惠券功能预留'] },
{ ver: '1.5.2', date: '2026-09-17', notes: ['作品板块真实案例改版：文案馆 4 张占位图换成完整文案案例（公众号节气推文 / 小红书文具种草 / 民宿品牌故事 / 毕业致辞演讲稿，正文可读、数据标注齐全）', '演示分类 2→4 张：新增路演 BP 页（市场+单店模型）、年终述职页（深色 KPI 大数字+季度时间线）', '数据分类 2→4 张：新增清洗前后对比（乱表→规范表）、进销存多表汇总透视（周转预警）', '视频脚本 2 张重做：28 秒分镜脚本（逐镜台词+时长+BGM 卡点）、60 秒口播脚本（时间轴+停顿重音+完播结构）', '主图改为完整展示（contain），案例样张的标题和交付说明不再被裁掉', '旧示例图链接自动升级版本号，老访客不再看到缓存的旧图'] },
{ ver: '1.5.1', date: '2026-09-17', notes: ['【安全】修复邀请码生成接口报错（功能恢复正常）', '【安全】管理端用户列表不再返回密码哈希/身份证哈希/支付宝账号', '【安全】写手端两处跨用户脚本注入（消息横幅/删除好友）修复', '【安全】游戏结算补上限校验与限流，翻翻乐并发重复入账修复', '【安全】部署自检接口只回概要，完整清单仅管理员可见', '【安全】服务器请求体/上传/单包上限下调，防止内存被打爆', '【安全】同步脚本不再内置 GitHub 令牌，改为读环境变量', '【修复】管理员新公告不再被默认内容覆盖；数据库断线自动恢复；提现审批搜索可用；游戏工作台魔法球数字恢复显示；换图后客户端缓存自动更新', '【修复】签到/月度奖励/提现申请/邀请码并发重复领取全面加锁', '【优化】接口报错不再暴露内部细节；门户注册的写手补齐工号/合同/欢迎信', '【提速】全站图片压缩瘦身 35MB→6.6MB；服务器开启 gzip（网页再省60%+）', '【提速】聊天图片加载后停止骨架屏动画；消息/卡片批量更新合并重绘，告别卡顿掉帧'] },
{ ver: '1.5.0', date: '2026-09-16', notes: ['用户端「作品 · 能做什么」重做：左侧服务说明卡（矢量图标 + 卖点清单）+ 右侧主视觉大图 + 缩略图条，点缩略图 / 箭头 / 键盘切换，点大图看原图', '四个分类图标由表情符号换成内联 SVG 矢量图', '新增 8 张设计样张（文案 / 演示 / 数据 / 视频 各 2 张 SVG），后台还没上传案例的分类自动用样张补位，传了真实案例就自动替换', '小沐AI 悬浮球改为扁平纸感印章风（圆角方形 + 线描气泡图标 + 朱红未读点），替掉之前的伪 3D 玻璃球', '悬浮球支持拖动：位置自动记忆、靠边自动吸附、双击复位，对话面板跟随球的位置展开'] },
{ ver: '1.4.0', date: '2026-09-16', notes: ['用户端宣传页交互改版：进站丝滑入场动画、登录注册口袋式开合弹窗、作品×能做什么合并成一块（分类切换 + 案例左右滑动）、小沐AI 改为右下角悬浮窗', '案例新增分类字段，后台「作品管理」可选择分类', '部署自检升级：覆盖 37 个文件，接口直接列出缺失的文件', '【安全】数据库连接串改为环境变量读取，不再硬编码在代码里'] },
{ ver: '1.2.2', date: '2026-09-16', notes: ['【重要修复】登录失败：新登录口漏了主体系的密码SHA-256预哈希约定，导致老账号全部对不上——已修并双兼容', '统一登录回到原登录页（保留人机验证），登录后按身份自动分流', '注册邀请码改为选填：不填=用户端，填写=写手（带人机验证）', 'portal 顶栏登录/注册按钮直接跳转，不再用弹窗'] },
{ ver: '1.2.1', date: '2026-09-16', notes: ['统一登录：网站宣传端一个入口，管理员/写手/用户登录自动分流', '统一注册：选填邀请码——填了进写手端，不填进用户端', '管理功能归位：dispatch.html 为管理员后台（+套餐/台账入口），index.html 纯台账', '山海：技能条中文修复（御剑术+剑诀层数）、原版横幅恢复、飞剑加剑光拖尾、卸武器保留基础攻击5'] },
{ ver: '1.2.0', date: '2026-09-16', notes: ['全新用户端上线：文案馆/接单介绍卡组/套餐方案/小沐AI/个人中心', '网站首页直达用户端，用户可开放注册', '写手注册仅限邀请码，旧账号登录自动分流到对应端', '管理员端新增：用户端套餐配置+用户咨询跟进', '新增部署自检接口 /api/deploy-check（治上传不完整）', '山海：飞剑玩法与美术复验通过'] },
{ ver: '1.1.1', date: '2026-09-15', notes: ['【重要】修复缓存根疾：此前 js/css/图片被浏览器缓存30天且地址不变，导致部署新版后手机一直用旧资源', '山海：星星/横幅/头像框全部重绘（完美透明）', '御剑五诀技能卡置顶展示', '山海：空装备槽显示半透明装备剪影', '装备页/关卡页实拍验收通过'] },
{ ver: '1.1.0', date: '2026-09-15', notes: ['聊天图片自动压缩（30MB上限）', '红包领取免手动刷新', '顶部邮箱改为广告入口', '游戏资源强制预下载+秒开缓存', '山海：新首页美术+装备图标+寻宝挪山海录', '御剑术玩法：初始飞剑+剑诀五技能', '新增：更新检查/缓存清理/资源包下载'] }
];
