# 配置模板：复制为 .env 使用，或直接在服务器后台配环境变量（二选一，推荐后者）
# ⚠️ 这个文件不要提交到 GitHub

# 1) 数据库连接串【必填】—— 没配服务会直接停下并打印配置指引
#    从 MongoDB Atlas 后台取：Database → Connect → Drivers
MONGO_URI=mongodb+srv://用户名:密码@集群地址/invest-jiedanyuan?retryWrites=true&w=majority

# 2) 数据库名（连接串里已含，一般不用单独配）
# MONGO_DB=invest-jiedanyuan

# 3) 登录令牌签名密钥【强烈建议配】
#    一串 32 位以上随机字符。不配也能跑，但每次重启服务所有人都要重新登录
JWT_SECRET=请换成一串你自己生成的32位以上随机字符

# 4) 服务端口（Render 会自动注入 PORT；本地跑默认 3000）
# PORT=3000

# 5) 部署在 nginx / 宝塔等反向代理后面时设为 1，限流才能拿到访客真实 IP
# TRUST_PROXY=1
